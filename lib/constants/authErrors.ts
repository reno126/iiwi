/**
 * Standardized unauthorized error message returned by server actions
 * when a user session is missing or invalid.
 * Safe to import in both Client and Server Components.
 */
export const UNAUTHORIZED_ERROR_MESSAGE = "UNAUTHORIZED";
